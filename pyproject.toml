[project]
name = "maze-consistency"
version = "0.1.0"
description = "Reward-conditioned self-consistency on a fixed maze (JAX)"
authors = [
    { name = "amdson", email = "amdickson128@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "flax>=0.12.8",
    "jax>=0.10.2",
    "matplotlib>=3.11.1",
    "numpy>=2.4.6",
    "optax>=0.2.8",
]

[tool.uv]
package = false
